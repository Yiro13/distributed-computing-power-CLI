syntax = "proto3";

service ClientServerComm {
    rpc KeepAlive(stream ClientStatus) returns (stream ServerResponse);
}

message ClientStatus {
    string client_id = 1;
    string status = 2; // "active"
}

message ServerResponse {
    string message = 1;
}
